# **FAT16简易文件系统模拟器** 核心算法描述

## 创建新文件流程：
1. 调用touch
2. 分配空白FCB
3. 将数据内容按簇写入

### 新文件数据写入过程：
1. 找到第一个空簇
2. 写入这个首簇号到FCB，写入文件
3. 若需要更多簇，查找下一个空簇
4. 空簇号写入当前数据簇簇号对应FAT表项中，并将余下文件数据写入空簇
5. 文件写入完毕，最后一簇对应FAT表项置为0xFFFF

## 文件删除流程：
1. 读取文件对应FCB，从首簇号开始，读出下一簇号
2. 将当前簇号对应FAT表项置0，并将下一簇号作为当前簇号
3. 重复上述过程直到遇见FAT表项为0xFFFF的表项，将其也置0
4. 回收这个FCB

## 文件路径解析实现原理

### 文件路径规范
1. / 表示根目录
2. . 表示当前目录
3. .. 表示上一级目录
4. 相对路径从当前目录出发，不以 / 开头
5. 绝对路径以 / 开头从根目录出发
6. 多个连续 / 视为单个 /
7. / 作为目录名分隔符
8. 只要不是根目录，路径名末尾的 / 视为空

### 实现原理
1. 判断路径类型为绝对路径还是相对路径
2. 利用strtok()之类的方法以 / 为分隔符将路径字符串分割为一系列目录名，依 1 决定是否添加 / 表示起始从根目录开始
3. 化简路径
    1)  维护两个栈，一个栈存放 2 中生成的一系列目录名token，另一个栈存放结果
    2)  出栈，若为 . 直接删除，若为 .. 删除结果栈栈顶内容
    3)  第一个栈空时，第二个栈（结果栈）即为最终化简的路径
4. 若为相对路径，拼接当前路径即可
5. 最后得到的是从根目录出发的绝对路径